# StackSup

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add stack_sup to your list of dependencies in `mix.exs`:

        def deps do
          [{:stack_sup, "~> 0.0.1"}]
        end

  2. Ensure stack_sup is started before your application:

        def application do
          [applications: [:stack_sup]]
        end
